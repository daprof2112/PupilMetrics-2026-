name: pupilmetrics_license_server
description: PupilMetrics license activation and validation server
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.3.2 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  crypto: ^3.0.3
